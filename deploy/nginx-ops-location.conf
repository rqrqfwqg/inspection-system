# ============================================================
# inspection-system · nginx 片段
# 用法：不要新建 server（443 已被物料系统占用，会端口冲突）
#      把下面内容插进现有 443 的 server { } 块内即可（location / 之前）
# 覆盖：/ops/ 页面  /ops/api 接口  /ops/assets 静态  /ops/uploads 照片
# ============================================================

location /ops/ {
    proxy_pass http://127.0.0.1:9527;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Prefix /ops;
    proxy_redirect off;
    proxy_read_timeout 120s;
    # 现场照片上传（单张 ≤10MB）
    client_max_body_size 50m;
}

# WebSocket 如需（巡检推送）；当前未用到，保留备用
# location /ops/ws {
#     proxy_pass http://127.0.0.1:9527;
#     proxy_http_version 1.1;
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection "upgrade";
# }
